export default function Home() {
    return <div>
    <h1>Each page is a different route. <br>
        
    </br>
    On URL use domain.com/filename (in this case localhost:3000/example)</h1>
    </div>;
  }